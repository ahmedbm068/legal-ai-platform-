# Advancement Logs

This folder stores automatic progress logs.

- On every GitHub push, a workflow generates a new markdown file here summarizing:
  - commits in that push
  - changed areas
  - changed files
- Agent evaluation reports are written to `advancement/evals/` by `scripts/run_agent_evals.py`.

